secrets:
  email_imap_password:
    file: ./secrets/email_imap_password.txt

services:
  goimapnotify:
    image: ghcr.io/n0rthernl1ghts/goimapnotify:latest
    init: true
    build: 
      context: .
    volumes:
      - ./config/goimapnotify/config.yaml:/etc/imapnotify/config.yaml
    secrets:
      - email_imap_password
